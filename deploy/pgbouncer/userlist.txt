# Replace md5<hash> with the actual md5 hash of your PostgreSQL password.
# Generate with: echo -n "passwordchatsem" | md5sum  (password + username, no separator)
# Format: "username" "md5<hash>"
"chatsem" "md5REPLACEME"
